---
- name: "packages | check facts"
  package_facts:
    manager: apt

- name: "packages | install packages"
  apt:
    pkg:
      - default-libmysqlclient-dev
      - libmagic-dev
      - libxml2-dev
      - libxslt-dev
      - pkg-config
      - rbenv
      - redis
      - yarnpkg

